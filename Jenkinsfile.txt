pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Performing Test Build..'
                git url: 'https://github.com/Debolina1110/MyfirstRepo.git', branch: 'main'
                credentialsId: 'jenkins-integration'
                sh "chmod +x -R ./test.sh"
                sh "./test.sh"
            }
        }
    }

}        
        